const wow = 3;

fn main(argv) {
    let x = 2;
    let y = true;
    let b = !false;
    let z = (2 * 2) / ((3 - 4) * -2);
    let a = (2 * 2) / 3;
    let c = 2 * a;
    print("test");
    print(1.3);

    print(wow);

    print(x, x);
    print(2);

    test();
}

fn test(){
    if true {

    } else {
// comment
        print(2);
    }
}

fn new_function(arg1, arg2, arg3) {
{

    test();
}
}
